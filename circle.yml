machine:
  java:
    version: oraclejdk8
  environment:
    JUNIT_PATH: $CIRCLE_TEST_REPORTS
test:
  override:
    - mvn verify
  post:
    - chmod +x ./$CIRCLE_TEST_REPORTS
    - mkdir -p ./$CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
deployment:
  master:
    branch: master
  tags:
    tag: release_*
    commands: 
      - pwd
