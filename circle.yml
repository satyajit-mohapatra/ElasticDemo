machine:
  java:
    version: oraclejdk8
  environment:
    JUNIT_PATH: $CIRCLE_TEST_REPORTS
test:
  override:
    - if [[ $CIRCLE_BRANCH != "" ]] ; then mvn -P docs verify ; fi
deployment:
  master:
    branch: master
    commands:
      - export data=234
      - echo ${DATA}
  release:
    tag: /release_.*/
    commands: 
      - pwd
